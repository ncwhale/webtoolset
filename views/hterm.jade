doctype html
html
  body(style='margin:0')
    #terminal(style='position:absolute; width:100%; height:100%')

    script(src='js/lib/hterm_all.js')
    script.
      hterm.defaultStorage = new lib.Storage.Local();
      var t = new hterm.Terminal();
      t.onTerminalReady = function() {
      /*
        var io = t.io.push();

        io.onVTKeystroke = function(str) {
        // Do something useful with str here.
        // For example, Secure Shell forwards the string onto the NaCl plugin.
          console.log('VTKey:'+str);
        };

        io.sendString = function(str) {
        // like a keystroke, except str was generated by the
        // terminal itself.
        // Most likely you'll do the same this as onVTKeystroke.
          console.log('Send:'+str);
        };
  
        io.onTerminalResize = function(columns, rows) {
        // React to size changes here.
        // Secure Shell pokes at NaCl, which eventually results in
        // some ioctls on the host.
          console.log('Resize:' + columns + 'x' + rows);
        };
  
        // You can call io.push() to foreground a fresh io context, which can
        // be uses to give control of the terminal to something else.  When that
        // thing is complete, should call io.pop() to restore control to the
        // previous io object.
        /**/
        t.io.println('Terminal Ready.');
      };
      t.decorate(document.querySelector('#terminal'));

